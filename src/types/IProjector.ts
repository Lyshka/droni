export type TypeProjector = {
  title_projector: string;
  description_projector: string;
  img_projector: number;
  img_mobile_projector: number;
};

export interface IProjector {
  acf: TypeProjector;
}
